---
category: Blocks
---

# TextImage

A short paragraph paired with an image. `stacked` gives the image full width above the text; `side` places them in two columns (alternate `imageSide` down the email).
